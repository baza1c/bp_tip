#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Записывает значение константы при обновлении ИБ
//
Процедура УстановитьКонстантуПриОбновленииИнформационнойБазы() Экспорт
	
	МенеджерКонстанты = СоздатьМенеджерЗначения();
	
	ЗначениеКонстанты = ОбщегоНазначенияБП.ЭтоБизнесСтарт()
		И Константы.ИспользоватьДокументыРеализации.Получить();
		
	Если ЗначениеКонстанты
		И ПолучитьФункциональнуюОпцию("ИспользоватьСинхронизациюДанных") Тогда
		ЗначениеКонстанты = ОбменДаннымиСервер.УзлыОбменаБСП().Количество() = 0;
	КонецЕсли;
	
	МенеджерКонстанты.Значение = ЗначениеКонстанты;
	ОбновлениеИнформационнойБазы.ЗаписатьДанные(МенеджерКонстанты);
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли